import { StyleSheet } from 'react-native';

const CardStyles = StyleSheet.create({
  card: {
    marginBottom: 10,
    marginHorizontal: 10,
    borderRadius: 8, // Ajuste conforme necessário
    elevation: 3,
    borderColor: '#004080', // Cor da borda neon
    borderWidth: 1, // Largura da borda
  },
});

export default CardStyles;
